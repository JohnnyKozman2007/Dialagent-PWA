name: my_restaurant_app
description: "A restaurant management PWA"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  crypto: ^3.0.3
  base32: ^2.0.0
  qr_flutter: ^4.1.0
  flutter_riverpod: ^2.5.0
  go_router: ^14.0.0
  shared_preferences: ^2.3.0
  equatable: ^2.0.5
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # 🔥 NO assets section – remove it entirely
