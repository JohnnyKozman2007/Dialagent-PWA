name: my_restaurant_app
description: A restaurant management PWA with Firebase.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase – use versions that are known to work together
  firebase_core: ^2.32.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.4
  cloud_functions: ^5.1.3

  # Routing & State
  go_router: ^14.6.1          # updated to latest stable that works with Flutter 3.22+
  flutter_riverpod: ^2.6.1

  # UI & Helpers
  equatable: ^2.0.5
  intl: ^0.19.0
  flutter_dotenv: ^5.2.1

  # 2FA (TOTP) – add base32
  qr_flutter: ^4.1.0
  crypto: ^3.0.3
  base32: ^2.1.3              # <-- ADD THIS

  # Web specific – for session storage and HTML
  universal_html: ^2.2.4

  # PWA support
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
